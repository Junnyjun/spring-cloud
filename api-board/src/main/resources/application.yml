server:
  port: 8081

spring:
  application:
    name: board-Service
  profiles:
    active: dev


eureka:
  instance:
    hostname: sub.junnyland.com
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://sub.junnyland.com:8761/eureka
